# Project Conventions

- Editable scope is restricted by `editable_paths` in project config.
- Avoid changing generated folders and dependency folders.
- Keep fixes minimal and reversible; one root-cause per iteration.
